{
  "mcpServers": {
    "readable": {
      "command": "uv",
      "args": [
        "--directory",
        "/absolute/path/to/readable-mcp",
        "run",
        "readable-mcp"
      ]
    }
  }
}
